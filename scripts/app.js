console.log("Lekan is Live");

const contact = document.getElementById("contact__details");
const projectName = document.getElementsByClassName("project__title");
const projectNames = ["Airbnb Clone", "Netflix Clone"];

// contact.innerHTML = "<h1>Contact Form is not ready yet!!</h1> <button id='view'>Check Progress</button>";

const viewContactForm = document.getElementById("view");

viewContactForm.addEventListener("click", () => {})

projectNames.map(project => {
    projectName.innerHTML 
})
